{
  "name": "gemfort-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "tsx --test src/notifications/__tests__/types.test.ts",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google/genai": "^2.12.0",
    "@mendable/firecrawl-js": "^4.30.1",
    "date-fns": "^4.1.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "tsx": "^4.20.3",
    "typescript": "^5.9.3"
  }
}
